---
const { href, title, body, thumbnail } = Astro.props;
---
<div class="goods">
<article>
<a href={href}>
  <h2>{title}</h2>
  <img src=`/images/${thumbnail}` alt={title} />
  <p>{body}</p>
</a>
</article>
</div>